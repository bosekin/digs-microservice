apiVersion: apps/v1
kind: Deployment
metadata:
  name: digs-microservice-deployment
  labels:
    app: digs-microservice
spec:
  replicas: 1
  selector:
    matchLabels:
      app: digs-microservice
  template:
    metadata:
      labels:
        app: digs-microservice
    spec:
      imagePullSecrets:
      - name: docker-hub-private
      containers:
      - name: digs-microservice
        image: kingslysg/digs-microservice
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: digs-microservice-ngix
  labels:
    app: digs-microservice-ngix
spec:
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    nodePort: 31515
  type: NodePort
  selector:
    app: digs-microservice-srv